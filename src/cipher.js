//cipher.encode(offset, string)
//offset é numero de posiçoes que user deseja mover pra DIREITA no alfabeto
//string é a mensagem (texto) que user deseja CIFRAR
//cipher.decode(offset, string)
//offset é o número de posições que queremos mover para a ESQUERDA no alfabeto 
//string é a mensagem (texto) que queremos DECIFRAR

const cipher = {
  encode: function encode(key, encodeWord) {
   

export default cipher;